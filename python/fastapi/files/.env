# FastAPI app env (copied into apps/api/.env)

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=ChangeMeLooksLikeARealPassword

# Optional: override if you want (alembic/env.py uses settings.DATABASE_URL)
# DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/postgres
