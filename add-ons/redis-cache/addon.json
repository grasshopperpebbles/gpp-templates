{
  "name": "redis-cache",
  "displayName": "Redis Cache",
  "description": "Add Redis-backed caching with in-memory fallback for API projects",
  "version": "1.0.0",
  "platforms": ["api"],
  "variants": {
    "api": ["fastapi", "express"]
  },
  "dependencies": {
    "fastapi": {
      "requirements": ["redis>=5.0.1"]
    },
    "express": {
      "package.json": {
        "dependencies": {
          "redis": "^4.6.0"
        }
      }
    }
  }
}

