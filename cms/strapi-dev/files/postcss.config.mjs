// PostCSS config for Strapi 5
// Strapi uses Vite internally, which requires a PostCSS config
// This minimal config prevents Vite from loading parent directory's Tailwind config
export default {
  plugins: [],
};

