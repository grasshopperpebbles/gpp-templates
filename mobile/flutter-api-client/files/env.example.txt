# Flutter API Client Environment Configuration
# Copy this to .env and update with your values
# Note: Flutter doesn't natively support .env files - use --dart-define or a package like flutter_dotenv

# API Configuration
API_BASE_URL=https://api.example.com

# Environment
PRODUCTION=false
