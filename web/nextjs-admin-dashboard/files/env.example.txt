# API Configuration
# Public browser-facing API base URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# Optional internal server-side API base URL for NextAuth/server components.
API_URL=http://localhost:8000/api/v1

# GraphQL (optional - for FastAPI, Strapi, etc.)
NEXT_PUBLIC_GRAPHQL_ENDPOINT=http://localhost:8000/graphql

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-change-in-production

# Prisma Schema (for CRUD generation)
# If using shared schema in monorepo:
PRISMA_SCHEMA_PATH=../packages/prisma-schema/schema.prisma

# Site Configuration
NEXT_PUBLIC_SITE_NAME=Admin Dashboard
