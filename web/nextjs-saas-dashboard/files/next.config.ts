import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // API proxy configuration can be added here if needed
  // For now, we'll use direct API calls from the client
};

export default nextConfig;
