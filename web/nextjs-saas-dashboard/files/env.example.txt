# API Configuration
# Public browser-facing API base URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# Optional internal server-side API base URL for NextAuth/server components.
API_URL=http://localhost:8000/api/v1

# GraphQL (optional - for FastAPI, Strapi, etc.)
NEXT_PUBLIC_GRAPHQL_ENDPOINT=http://localhost:8000/graphql

# Authentication
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-change-in-production

# Real-time (optional)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Feature Flags
NEXT_PUBLIC_ENABLE_REALTIME=true
NEXT_PUBLIC_ENABLE_ANALYTICS=true

# Multi-tenant Features (opt-in, off by default)
NEXT_PUBLIC_ENABLE_TEAMS=false
NEXT_PUBLIC_ENABLE_BILLING=false

# Site Configuration
NEXT_PUBLIC_SITE_NAME=SaaS Dashboard
