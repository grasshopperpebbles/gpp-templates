import StoryblokComponent from '@storyblok/svelte';

// Define your Storyblok components mapping
export const components = {
  page: StoryblokComponent,
  article: StoryblokComponent,
  teaser: StoryblokComponent,
  grid: StoryblokComponent,
  feature: StoryblokComponent,
  // Add more component mappings as needed
};

