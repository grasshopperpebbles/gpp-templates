# Server Configuration
NODE_ENV=development
API_PORT=3001

# Database — DATABASE_URL belongs in .env (not here) so Prisma CLI can read it.
# See .env.example for the database connection string.

# JWT Authentication
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS (comma-separated origins for production)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Optional: External Services
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# RESEND_API_KEY=
# STRIPE_SECRET_KEY=
